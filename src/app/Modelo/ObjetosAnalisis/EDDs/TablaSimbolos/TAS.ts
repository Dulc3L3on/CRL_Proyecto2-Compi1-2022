export class TAS{
    //la parte globla, estaba pensndo que solo debería almacenar objetos variable, hasta llegué a pensar que
    //en las mismas instancias de las instrucciones podría almacenar el valor, pero para que seaa todo más
    //segmentado y especializado, mejor que exista esta TAS
        //esta área se debe llenar antes de el ele del listado de exe sea pasado al stack y este de su listado
        //completo, puesto que es una f, ciclo, o sentControl, para que sea exe
        //y debe llenarse después de parsear la clase y con ello obtener el listado de instrucciones...

    //también estaba pensando que el "Heap", no sería necesario, addlo aquí, puesto que con el listado de 
    //"por ejecutar" basta para crear el stack, quien será el que contenga el orden de ejecución...


    draw(){
        
    }

}